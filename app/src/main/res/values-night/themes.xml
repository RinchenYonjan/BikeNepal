<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.BikeNepal" parent="Theme.AppCompat.DayNight.NoActionBar">

    </style>

    <!-- Dark Mode -->
    <!-- Screen background color if dark mode on -->
    <style name="SCREEN">
        <item name="android:background">#1B1B1B</item>
    </style>

    <!-- Background color if dark mode on -->
    <style name="BACKGROUND">
        <item name="android:background">#1C252E</item>
    </style>

    <!-- Edit Text background color if dark mode on -->
    <style name="EDITTEXT">
        <item name="android:color">#FFF</item>
        <item name="android:textColor">#000</item>
    </style>

    <!-- Texts color if dark mode on -->
    <style name="TEXT">
        <item name="android:textColor">#FFF</item>
    </style>

    <!-- Button background color if dark mode on -->
    <style name="BUTTON">
        <item name="android:background">@null</item>
        <item name="android:textColor">#FFF</item>
    </style>

</resources>
